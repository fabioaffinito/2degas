# make defs for Galileo
F90=mpif90
BLAS_INTERNAL=
EXE=rept90-mpi.x
#
# other fpp flags include -DDEBUG
#FPP=-fpp -DBLAS_INTERNAL -DDEBUG
F90FLAGS= -O3 -g -fpp
#F90FLAGS= -O3 -g -qopt-report

ifdef BLAS_INTERNAL
   EXE=rept90-int.x
   FPP:= $(FPP) -DBLAS_INTERNAL
   LDFLAGS=
else
   F90_OBJS:= tools.o $(F90_OBJS)
   LDFLAGS=-mkl
endif

